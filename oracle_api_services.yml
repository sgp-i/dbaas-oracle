- name: checking api services for oracle
  hosts: "{{ host }}"
  become: yes
  become_method: sudo

  tasks:
    - import_role:
        name: oracle_api_services
        tasks_from: host_inventory_insertion.yml
    - debug:
        msg: Host inserted
    - import_role:
        name: oracle_api_services
        tasks_from: host_inventory_update.yml
  vars:
    host_code: scottTest1
