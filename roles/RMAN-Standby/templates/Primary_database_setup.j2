alter database force logging;
alter system set log_archive_config='DG_CONFIG=({{ Source_DB }},{{ TARGET_DB }})' scope=both;
alter system set log_archive_dest_2='SERVICE={{ TARGET_DB }} noaffirm async valid_for=(online_logfiles,primary_role) db_unique_name={{ TARGET_DB }}'  scope=both;
alter system set log_archive_dest_state_2=enable  scope=both;
alter system set fal_server={{ TARGET_DB }}  scope=both;
alter system set standby_file_management=auto  scope=both;

alter database add standby logfile '+RECO' size 50m;
alter database add standby logfile '+RECO' size 50m;
alter database add standby logfile '+RECO' size 50m;
alter database add standby logfile '+RECO' size 50m;
