alter database force logging;
alter system set log_archive_config='DG_CONFIG=({{ Source_DB_NAME }},{{ TARGET_DB_NAME }})' scope=both;
alter system set log_archive_dest_2='SERVICE={{ TARGET_DB_NAME }} noaffirm async valid_for=(online_logfiles,primary_role) db_unique_name={{ TARGET_DB_NAME }}'  scope=both;
alter system set log_archive_dest_state_2=enable  scope=both;
alter system set fal_server={{ TARGET_DB_NAME }}  scope=both;
alter system set standby_file_management=auto  scope=both;

alter database add standby logfile '+RECO' size 50m;
alter database add standby logfile '+RECO' size 50m;
alter database add standby logfile '+RECO' size 50m;
alter database add standby logfile '+RECO' size 50m;
